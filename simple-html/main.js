linkExterno.addEventListeer('Click', (e) => {
    const opition = 
       confirm = ("Você será Redirecioando para um Link Externo");
       if (opition === false) {
        e.preventDefault();
    
       }
});